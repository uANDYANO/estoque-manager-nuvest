💡 Quando for abrir o projeto em outro PC:

   1- Clonar o repositório:

git clone https://github.com/seu-usuario/estoque-manager.git
cd estoque-manager

   2- Criar um novo ambiente virtual e instalar as dependências Python:

python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

   3- Instalar os pacotes do Node:

npm install

   4- Rodar normalmente com:

python app.py